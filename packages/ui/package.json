{
  "name": "@kiwiply/ui",
  "version": "0.0.0",
  "private": true,
  "description": "Shared UI for the Kiwiply web app and browser extension: design tokens now; React primitives + the ResumeUpload form land in W1.2/W1.3. Consumed as source by both Next (web) and Vite/WXT (extension) — no build step.",
  "type": "module",
  "sideEffects": [
    "*.css"
  ],
  "exports": {
    "./styles/tokens.css": "./styles/tokens.css"
  },
  "files": [
    "styles",
    "src"
  ]
}
