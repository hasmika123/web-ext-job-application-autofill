# Secrets — NEVER commit. Real values go in .env on the server; the template is tracked.
.env
.env.*
!.env.example

# OS / editor cruft
.DS_Store
Thumbs.db

# Local Claude Code tooling (per-machine launch config; not shared)
.claude/
