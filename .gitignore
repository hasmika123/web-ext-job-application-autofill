# Secrets — NEVER commit. Real values go in .env on the server; the template is tracked.
.env
.env.*
!.env.example

# Workspace root install (npm workspaces). The root package-lock.json IS committed.
node_modules/

# OS / editor cruft
.DS_Store
Thumbs.db

# Local Claude Code tooling (per-machine launch config; not shared)
.claude/
