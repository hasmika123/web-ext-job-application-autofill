# ===================================================================
# Spring Boot configuration for the "prod" profile.
#
# This configuration overrides the application.yml file.
#
# More information on profiles: https://www.jhipster.tech/profiles/
# More information on configuration properties: https://www.jhipster.tech/common-application-properties/
# ===================================================================

# ===================================================================
# Standard Spring Boot properties.
# Full reference is available at:
# http://docs.spring.io/spring-boot/docs/current/reference/html/common-application-properties.html
# ===================================================================

logging:
  level:
    ROOT: INFO
    tech.jhipster: INFO
    com.dossier.api: INFO

management:
  prometheus:
    metrics:
      export:
        enabled: false

spring:
  devtools:
    restart:
      enabled: false
    livereload:
      enabled: false
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    url: jdbc:mysql://localhost:3306/dossierApi?useUnicode=true&characterEncoding=utf8&useSSL=false&useLegacyDatetimeCode=false&createDatabaseIfNotExist=true
    username: root
    password:
    hikari:
      poolName: Hikari
      auto-commit: false
      data-source-properties:
        cachePrepStmts: true
        prepStmtCacheSize: 250
        prepStmtCacheSqlLimit: 2048
        useServerPrepStmts: true
  # Replace by 'prod, faker' to add the faker context and have sample data loaded in production
  liquibase:
    contexts: prod
  mail:
    # SMTP for verification emails (Brevo now; provider-agnostic — just env). Defaults keep
    # the app booting with no mail configured (send simply fails, logged async).
    host: ${MAIL_HOST:localhost}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  thymeleaf:
    cache: true

# ===================================================================
# To enable TLS in production, generate a certificate using:
# keytool -genkey -alias dossierapi -storetype PKCS12 -keyalg RSA -keysize 2048 -keystore keystore.p12 -validity 3650
#
# You can also use Let's Encrypt:
# See details in topic "Create a Java Keystore (.JKS) from Let's Encrypt Certificates" on https://maximilian-boehm.com/en-gb/blog
#
# Then, modify the server.ssl properties so your "server" configuration looks like:
#
# server:
#   port: 443
#   ssl:
#     key-store: classpath:config/tls/keystore.p12
#     key-store-password: password
#     key-store-type: PKCS12
#     key-alias: selfsigned
#     # The ciphers suite enforce the security by deactivating some old and deprecated SSL cipher, this list was tested against SSL Labs (https://www.ssllabs.com/ssltest/)
#     ciphers: TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA,TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA,TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256,TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384 ,TLS_DHE_RSA_WITH_AES_128_GCM_SHA256 ,TLS_DHE_RSA_WITH_AES_256_GCM_SHA384 ,TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA256,TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA384,TLS_DHE_RSA_WITH_AES_128_CBC_SHA256,TLS_DHE_RSA_WITH_AES_128_CBC_SHA,TLS_DHE_RSA_WITH_AES_256_CBC_SHA256,TLS_DHE_RSA_WITH_AES_256_CBC_SHA,TLS_RSA_WITH_AES_128_GCM_SHA256,TLS_RSA_WITH_AES_256_GCM_SHA384,TLS_RSA_WITH_AES_128_CBC_SHA256,TLS_RSA_WITH_AES_256_CBC_SHA256,TLS_RSA_WITH_AES_128_CBC_SHA,TLS_RSA_WITH_AES_256_CBC_SHA,TLS_DHE_RSA_WITH_CAMELLIA_256_CBC_SHA,TLS_RSA_WITH_CAMELLIA_256_CBC_SHA,TLS_DHE_RSA_WITH_CAMELLIA_128_CBC_SHA,TLS_RSA_WITH_CAMELLIA_128_CBC_SHA
# ===================================================================
server:
  port: 8080
  shutdown: graceful # see https://docs.spring.io/spring-boot/docs/current/reference/html/spring-boot-features.html#boot-features-graceful-shutdown
  compression:
    enabled: true
    mime-types: text/html,text/xml,text/plain,text/css,application/javascript,application/json,image/svg+xml
    min-response-size: 1024

# ===================================================================
# JHipster specific properties
#
# Full reference is available at: https://www.jhipster.tech/common-application-properties/
# ===================================================================

jhipster:
  # CORS is needed for the BROWSER EXTENSION, which calls the API directly from its
  # chrome-extension:// origin. The web app does NOT need CORS (its browser only talks to
  # its own Next route handlers, which proxy to the API server-side). Override the pattern
  # with the published extension id at launch (e.g. chrome-extension://<id>).
  cors:
    allowed-origin-patterns: ${CORS_ALLOWED_ORIGIN_PATTERNS:chrome-extension://*}
    allowed-methods: GET,POST,PUT,PATCH,DELETE,OPTIONS
    allowed-headers: '*'
    exposed-headers: 'Authorization,Link,X-Total-Count'
    allow-credentials: true
    max-age: 1800
  http:
    cache: # Used by the CachingHttpHeadersFilter
      timeToLiveInDays: 1461
  cache: # Cache configuration
    ehcache: # Ehcache configuration
      time-to-live-seconds: 3600 # By default objects stay 1 hour in the cache
      max-entries: 1000 # Number of objects in each cache entry
  security:
    authentication:
      jwt:
        # This token must be encoded using Base64 and be at least 256 bits long (you can type `openssl rand -base64 64` on your command line to generate a 512 bits one)
        # As this is the PRODUCTION configuration, the secret has NO default here: prod must
        # supply it via the JHIPSTER_SECURITY_AUTHENTICATION_JWT_BASE64_SECRET environment
        # variable (generate one with `openssl rand -base64 64`). The app fails fast on boot
        # if it's missing, so prod can never silently run on a shared/committed key.
        base64-secret: ${JHIPSTER_SECURITY_AUTHENTICATION_JWT_BASE64_SECRET}
        # Access token is short-lived (15 min); refresh token is long-lived (90 days).
        # The web app silently rotates the refresh token (proxy.ts) on a rolling window,
        # so this is the max idle time before a re-login is required.
        token-validity-in-seconds: 900
        token-validity-in-seconds-for-remember-me: 7776000
  mail: # specific JHipster mail property, for standard properties see MailProperties
    # base-url is the WEB app URL — the activation link in the email points to
    # ${base-url}/account/activate?key=… (the Next page below), not the API.
    base-url: ${MAIL_BASE_URL:http://localhost:3000}
    from: ${MAIL_FROM:no-reply@localhost} # must be your Brevo-verified sender address
  logging:
    use-json-format: false # By default, logs are not in Json format
    logstash: # Forward logs to logstash over a socket, used by LoggingConfiguration
      enabled: false
      host: localhost
      port: 5000
      ring-buffer-size: 512
# ===================================================================
# Application specific properties
# Add your own application properties here, see the ApplicationProperties class
# to have type-safe configuration, like in the JHipsterProperties above
#
# More documentation is available at:
# https://www.jhipster.tech/common-application-properties/
# ===================================================================

application:
  liquibase:
    # Run Liquibase SYNCHRONOUSLY in prod (JHipster defaults to async). The AdminBootstrap
    # ApplicationRunner reads/writes jhi_user and MUST run after migrations complete — async
    # start would race it (and let the app serve traffic before the schema is migrated).
    async-start: false

# ===================================================================
# Dossier application properties (prod)
# ===================================================================
dossier:
  storage:
    # AWS S3 (private bucket). Leave the endpoint EMPTY so the SDK uses the default AWS
    # endpoint for the region; set it only for an S3-compatible store (MinIO/R2). region,
    # bucket and credentials come from the environment. path-style-access is FALSE for AWS
    # S3 (virtual-hosted style); it's a MinIO/R2 thing.
    endpoint: ${DOSSIER_STORAGE_ENDPOINT:}
    region: ${DOSSIER_STORAGE_REGION:us-east-1}
    bucket: ${DOSSIER_STORAGE_BUCKET:dossier-resumes}
    access-key: ${DOSSIER_STORAGE_ACCESS_KEY:}
    secret-key: ${DOSSIER_STORAGE_SECRET_KEY:}
    path-style-access: ${DOSSIER_STORAGE_PATH_STYLE_ACCESS:false}
  ai:
    # Server-side metered AI answer drafting (Phase 5.1). OFF unless enabled AND a key
    # is set. Provider-agnostic: switch provider/model/baseUrl without code changes.
    # The key lives here (env) only — it never ships in the extension bundle.
    # Gemini FREE-tier note: Google may use inputs to improve its services, so the
    # extension keeps this OPT-IN and the privacy policy discloses it. Switch to a paid
    # key (or another provider) to remove that data-use caveat.
    enabled: ${DOSSIER_AI_ENABLED:false}
    provider: ${DOSSIER_AI_PROVIDER:gemini}
    base-url: ${DOSSIER_AI_BASE_URL:https://generativelanguage.googleapis.com/v1beta}
    model: ${DOSSIER_AI_MODEL:gemini-2.5-flash-lite}
    api-key: ${DOSSIER_AI_API_KEY:}
    # Server AI is a Pro feature (Phase 12.4) — Free users bring their own key. The one ungated
    # exception is resume parsing: a Free user gets this many parses a month.
    free-monthly-quota: ${DOSSIER_AI_FREE_MONTHLY_QUOTA:50}
    # How AI is spent (Phase 13.1b; see AiPolicy). Pro is metered by COST from the ai_call ledger:
    # a monthly budget per user (an abuse ceiling, far above real use); past soft-cap-percent every
    # task switches to economy-model (blank = no switch); at 100 % it stops until the month resets
    # (UTC). An admin override is a budget too, and outranks the plan.
    policy:
      pro-monthly-budget-usd: ${DOSSIER_AI_PRO_MONTHLY_BUDGET_USD:5.0}
      soft-cap-percent: ${DOSSIER_AI_SOFT_CAP_PERCENT:80}
      economy-model: ${DOSSIER_AI_ECONOMY_MODEL:}
      # Per-feature kill switch, comma-separated: draft, pick, map, enrich, parse, match, fit, tailor, jobs, inbox.
      disabled-tasks: ${DOSSIER_AI_DISABLED_TASKS:}
      # Model per task; blank = dossier.ai.model.
      models:
        draft: ${DOSSIER_AI_MODEL_DRAFT:}
        pick: ${DOSSIER_AI_MODEL_PICK:}
        map: ${DOSSIER_AI_MODEL_MAP:}
        enrich: ${DOSSIER_AI_MODEL_ENRICH:}
        parse: ${DOSSIER_AI_MODEL_PARSE:}
        match: ${DOSSIER_AI_MODEL_MATCH:}
        # The ROADMAP routes the job-fit panel to Flash; unset, it runs on the default model.
        fit: ${DOSSIER_AI_MODEL_FIT:}
        # Resume tailoring (13.4) — also routed to Flash by the ROADMAP; unset = the default model.
        tailor: ${DOSSIER_AI_MODEL_TAILOR:}
        # Nightly job-match scoring (13.6b) — Flash-Lite by the ROADMAP; unset = the default model.
        jobs: ${DOSSIER_AI_MODEL_JOBS:}
        # Reading job emails the rules can't settle (14.4a) — Flash-Lite; unset = the default model.
        inbox: ${DOSSIER_AI_MODEL_INBOX:}
    max-output-tokens: ${DOSSIER_AI_MAX_OUTPUT_TOKENS:400}
    # Structured resume parsing (POST /api/ai/parse-resume) — full-resume JSON output,
    # so a much roomier cap than a 2-4 sentence draft. Same provider/model/key/quota.
    parse-max-output-tokens: ${DOSSIER_AI_PARSE_MAX_OUTPUT_TOKENS:4000}
  # Daily job matches, the source half (13.6a): read the public Greenhouse / Lever / Ashby job boards
  # in the pool every night (UTC) and keep postings from the last 48 h for 7 days. On in prod; off in
  # dev/test so nothing there calls the job boards. Admins can also run it from the admin page.
  jobs:
    enabled: ${DOSSIER_JOBS_ENABLED:true}
    fetch-cron: ${DOSSIER_JOBS_FETCH_CRON:0 0 2 * * *}
    # Daily matching for users who switched it on (13.6b), after the read.
    match-cron: ${DOSSIER_JOBS_MATCH_CRON:0 0 4 * * *}
  # The inbox's encryption key (14.2): AES-256-GCM for Gmail app passwords. Generate once with
  # `openssl rand -base64 32`, put it in the box's .env AND the password manager. Blank = inbox off.
  # Rotating: new key + key-version + 1, and move the old one under retired-keys (DEPLOY.md §12).
  inbox:
    key: ${DOSSIER_INBOX_KEY:}
    key-version: ${DOSSIER_INBOX_KEY_VERSION:1}
    # How often connected inboxes are read (14.3), and how far back a first read goes.
    poll-cron: ${DOSSIER_INBOX_POLL_CRON:0 */15 * * * *}
    backfill-days: ${DOSSIER_INBOX_BACKFILL_DAYS:60}
    # Stored mail is deleted this long after it was sent (14.7).
    retention-days: ${DOSSIER_INBOX_RETENTION_DAYS:365}
  stripe:
    # Billing (Phase 12). A BLANK secret key disables billing entirely: /api/billing/me still
    # answers (Free, billingEnabled=false) and checkout/portal return 503 BILLING_DISABLED, so
    # a server without these secrets runs normally — that's how develop and CI work.
    # Keys are env-only and never reach a client bundle. Store them in the password manager as
    # well as the box's .env: GitHub secrets are write-only and have never held our .env.
    secret-key: ${STRIPE_SECRET_KEY:}
    webhook-secret: ${STRIPE_WEBHOOK_SECRET:}
    price-monthly: ${STRIPE_PRICE_MONTHLY:}
    price3mo: ${STRIPE_PRICE_3MO:}
    # What each plan charges. Used ONLY for the admin MRR card (Phase 12.5) — Stripe owns the
    # real price and nobody is charged from here. Bump these together with the Stripe prices at
    # the Launch-2 rise (24.99 / 54.99); if they drift, the only thing that lies is that card.
    amount-monthly: ${STRIPE_AMOUNT_MONTHLY:19.99}
    amount3mo: ${STRIPE_AMOUNT_3MO:44.99}
    success-url: ${STRIPE_SUCCESS_URL:https://kiwiply.com/billing/success?session_id={CHECKOUT_SESSION_ID}}
    cancel-url: ${STRIPE_CANCEL_URL:https://kiwiply.com/pricing}
    portal-return-url: ${STRIPE_PORTAL_RETURN_URL:https://kiwiply.com/settings#billing}
  admin:
    # Real production admin, created/promoted from env at startup by AdminBootstrap (Phase 9.A0).
    # The default admin/admin seed no longer reaches prod; set these to install a real admin.
    # password-hash is a BCRYPT hash (NOT a plaintext password) and lives in env only — nothing
    # committed. Leave email/password-hash blank to skip bootstrap. Generate a hash with:
    #   htpasswd -bnBC 10 "" 'your-strong-password' | tr -d ':\n'
    email: ${ADMIN_EMAIL:}
    password-hash: ${ADMIN_PASSWORD_HASH:}
    login: ${ADMIN_LOGIN:admin}
    # Email-OTP MFA for admin sign-in (Phase 9.X.3). OFF by default. Turn on ONLY after confirming
    # admin email delivery works (Brevo) — otherwise admins can't receive the code. An admin with no
    # email on file is signed in without a code (never locked out). To recover if misconfigured, set
    # this back to false and recreate the api container.
    mfa-enabled: ${ADMIN_MFA_ENABLED:false}
    mfa-code-ttl-seconds: ${ADMIN_MFA_CODE_TTL_SECONDS:600}
  newsletter:
    # Physical postal address for marketing emails (CAN-SPAM / ePrivacy). Appears in the
    # newsletter footer. Leave blank to omit (set before sending real campaigns).
    postal-address: ${NEWSLETTER_POSTAL_ADDRESS:}
  brevo:
    # Brevo Contacts API sync (Phase 9.A4.4). OFF unless BOTH are set. The local email_subscriber
    # table stays the source of truth; confirmed subscribers are mirrored to the Brevo list and
    # unsubscribes are blacklisted there. The key is a Brevo API v3 key (env only, never bundled).
    api-key: ${BREVO_API_KEY:}
    list-id: ${BREVO_LIST_ID:0}
