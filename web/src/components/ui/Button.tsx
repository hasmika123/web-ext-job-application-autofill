/** Shared design-system Button (@kiwiply/ui) — this file only re-exports it so existing
 *  `@/components/ui/Button` imports keep working. */
export { Button as default, buttonVariants } from "@kiwiply/ui";
export type { ButtonProps, ButtonVariant, ButtonSize } from "@kiwiply/ui";
