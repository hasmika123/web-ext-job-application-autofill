/** Shared design-system Input + Field (@kiwiply/ui) — re-exported for `@/components/ui`
 *  imports. The shared Field also auto-wires ids/aria when `htmlFor` is omitted. */
export { Input as default, Field, inputClass } from "@kiwiply/ui";
export type { InputProps, FieldProps } from "@kiwiply/ui";
