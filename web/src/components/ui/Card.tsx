/** Shared design-system Card (@kiwiply/ui) — re-exported for `@/components/ui` imports.
 *  NOTE: the shared Card defaults to `padded` (p-6); pass `padded={false}` for a bare surface. */
export { Card as default, CardHeader, CardTitle, CardDescription, CardFooter, cardClass } from "@kiwiply/ui";
export type { CardProps } from "@kiwiply/ui";
