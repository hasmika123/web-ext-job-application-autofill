# EXT-UI-PLATFORM-PLAN.md — Extension UI platform (WXT) + shared review form

> **Branch:** `feat/shared-review-form` (all work for this build-out lives here).
> **Commit convention:** `w<phase>.<n>: <subject>` (e.g. `w0.2: WXT background entrypoint wraps the service worker`).
> One task ≈ one commit. PR per phase → CI → merge. Web phases deploy to prod; extension phases
> ride the eventual Chrome Web Store upload.
> Supersedes the narrower `SHARED-FORM-PLAN.md`. Companion to `ROADMAP.md` / `ADMIN-PLAN.md` / `PROGRESS.md`.

## Goal
Stand up a real **extension UI platform** so we can build rich UI at **Simplify scale and beyond**
— popup, options, side panel, injected on-page panels, shared state, all React + Tailwind and
**sharing components with the web app**. The first concrete payoff is **one shared resume-review
form** rendered in a right-side **Chrome Side Panel**; everything after builds on the same base.

## Strategy
Adopt **WXT** (Vite-based extension framework) as the build/UI foundation. **Keep the autofill
engine** — ATS adapters, `parser-core`, `schema`, `tracking`, `sync`, `job-capture`,
`app-tracking`, `field-cache`, content scripts, `rules` — **as imported modules** (they're good and
well-tested). WXT takes over the **build, manifest generation, and UI entrypoints**; UI converts to
React **incrementally**, starting with the new side panel — the existing popup/options keep working
throughout.

### Why WXT
Vite under the hood (aligns with the web's React/Tailwind stack → shared components drop in), file-based
entrypoints, **manifest generation**, **content-script UI with shadow-DOM** (how Simplify/Honey inject
panels), HMR, and **Chrome + Firefox from one codebase** (so the deferred Firefox parity becomes cheap).

## Decisions (2026-06-30, user-confirmed)
- **Framework = WXT** (Vite). Bundler-under-hood is Vite/esbuild; not hand-rolled.
- **Migration = engine stays, UI/build migrate** — incremental; engine modules imported, not rewritten.
- **Firefox = deferred** but near-free later via WXT's cross-browser build.
- **Shared UI = monorepo package** consumed by both web (Next) and the WXT extension.

## Architecture (target)
```
            ┌──────────────  packages/ui (or shared/)  ──────────────┐
            │  ResumeUpload + UI primitives + parser-core types       │  ← shared React + Tailwind
            └───────┬─────────────────────────────────────┬──────────┘
        web (Next)  │                                      │  extension (WXT / Vite)
                    ▼                                      ▼
   Resumes page + Add-app review            entrypoints/: popup · options · sidepanel · background · content
   onSave → /api/resumes/upload             sidepanel mounts <ResumeUpload onSave=…>
                                            engine modules (adapters/parser/tracking/sync) imported as-is
```
The form is **persistence-agnostic**: it emits `onSave(result)`. Web wires it to the upload route;
the extension wires it to `createResume`/`uploadResumeFile` or capture→`pushDraft`→`uploadApplicationAttachment`→fill.

## Locked-decision change (intentional)
The extension adopts a **build step / framework (WXT)**. The autofill **engine stays modular**; the
*build, manifest, and UI* are framework-managed. `CLAUDE.md` + `ARCHITECTURE.md` updated in W0.6 / W5.

---

## Phase W0 — Adopt WXT, port existing entrypoints **as-is** (lowest-risk foundation)
> WXT builds the *current* extension with **no behavior change** — popup/options/background/content
> all work unchanged, just relocated into WXT's structure with the manifest generated from config.
- [x] **W0.1** Add WXT to `job-autofill` (dev dep) + `wxt.config.ts` reproducing `manifest.json` (key, permissions, host_permissions, externally_connectable, content_scripts, icons, action, options→`options_ui{open_in_tab:true}`, web_accessible_resources, browser_specific_settings). *Toolchain landed in the prior `w0.1` commit; the config landed here in `w0.2`.*
- [x] **W0.2** `entrypoints/background.ts` — `defineBackground` wrapping `src/background/service-worker.js`; `importScripts` → ES side-effect imports of tracking/sync/app-tracking/analytics (bundled into one `background.js`). Logic unchanged.
- [x] **W0.3** `entrypoints/content.ts` — `defineContentScript` registering the existing bundle (rules→content-script, 18 files) in the SAME order, with the same matches / `all_frames` / `run_at`. Output: `content-scripts/content.js`.
- [x] **W0.4** `entrypoints/{popup,options,review}/` — plain HTML/JS entrypoints reusing the current popup/options/review files verbatim (no React yet); each `index.html` loads a `main.js` that side-effect-imports the libs in order, then the page script. `review` is an unlisted page (`getURL("review.html")`).
- [x] **W0.5** Engine modules importable under Vite confirmed (IIFEs self-attach to `globalThis`/`window` `.JAF` via side-effect imports); `cd job-autofill && npm test` stays **green (14 suites)**. Engine files untouched except removing the SW `importScripts` line (now done by the entrypoint).
- [x] **W0.6** `wxt build` produces a parity manifest + `.output/chrome-mv3` (gitignored — derivable from source; CI rebuilds + zips). Runtime-path fixes: popup/review `executeScript` → the bundled `content-scripts/content.js`; `getURL` → `options.html`/`review.html`; `vendor/` + `icons/` → `public/` (WAR-listed). `CLAUDE.md`/`ARCHITECTURE.md` updated. **Load-unpacked walkthrough (autofill/save-a-job/connect/bug/on-the-fly upload) is the one manual gate left — needs Chrome.**

## Phase W1 — Shared UI package (React + Tailwind)
> One source for the form + primitives, consumed by web and extension.
- [x] **W1.1** Create `packages/ui` (workspace) + the shared design tokens. **npm workspaces** at the repo root (`workspaces: ["packages/*"]`); `@kiwiply/ui` holds `styles/tokens.css` — the canonical brand palette + shape/elevation + Tailwind v4 `@theme inline` mapping, byte-identical to web's current inline tokens (zero-visual-change adoption). **Kept additive:** `web/` + `job-autofill/` are deliberately NOT workspace members yet (they still `npm ci` in isolation from their own lockfiles — verified `npm prefix`/`ci --dry-run` unchanged — so the LIVE web Docker/CI build is untouched). They join the workspace, consume the tokens, and get their Docker/CI install changes in **W1.4**. Root `package-lock.json` committed; root `node_modules/` gitignored.
- [x] **W1.2** Made `ResumeUpload` portable **in place** (still in `web/`). Injected a `ResumeUploadServices` prop set — `onSave(SaveInput)→SaveResult` (create/edit), optional `onUpdateProfile`, `track`, `toast`, `onRefresh` — and removed `useRouter` (`next/navigation`), the `track` import, `useToast`, and all three direct `fetch` calls; the component now owns only UX/flow. The exact web wiring (Next route handlers, GA4 `track`, toast, `router.refresh`) lives in a new `web/src/lib/use-resume-upload-services.ts` hook that both callers (`ResumesWorkspace`, `ApplicationBoard`'s `AddApplicationDialog`) spread. The "Detected contact"/base-profile panel is gated on `onUpdateProfile` (web passes it → unchanged; extension omits → hidden). **Zero web behavior change** — `tsc`+`eslint`+`next build` all green. No `next/image` was present. (Primitives/`parser-core`/`validate`/`cn` stay `@/` imports — they relocate in W1.3.)
- [x] **W1.3** Moved `ResumeUpload` (+ its sub-parts) into `@kiwiply/ui` (`packages/ui/src/ResumeUpload.tsx`) with self-contained copies of the primitives it needs (`Input`, `Button`/`buttonVariants`, `cn`, `LIMITS`) and the `parser-core` **types** (`parser-core-types.ts`). The last web-only runtime dep (`parseResume`) became an injected `parseFile` service. Package now ships React (peer dep) + a barrel `index.ts`. *(Primitive duplication is intentional — full unification is W5.1.)*
- [x] **W1.4 (web half)** Web consumes `@kiwiply/ui`: `web/` joined the workspace (`workspaces: ["packages/*","web"]`), imports the form from the package, `next.config` gained `transpilePackages:["@kiwiply/ui"]` + `turbopack.root`/`outputFileTracingRoot` pinned to the **repo root** (so Turbopack resolves the hoisted `next`; standalone output is monorepo-nested), and `globals.css` got an `@source` for the package. The **web Dockerfile + CI** are now workspace-aware (root `npm ci`, build `-w web`, nested-standalone runtime paths) + a new CI `web-docker` job builds the image as a smoke check. Vestigial `web/package-lock.json` removed (root lockfile governs). `tsc`+`eslint`+`next build` green; extension untouched. **The WXT (extension) half of W1.4 is deferred to W3** (the extension doesn't render the form until the side panel exists). **Not locally testable: the Docker image build — validated by the new CI `web-docker` job.**

## Phase W2 — Web consumes the shared package (parity, **deploys**)
- [x] **W2.1** Done as part of W1.4(web): `ResumesWorkspace` + `ApplicationBoard`'s Add-application review import `ResumeUpload` from `@kiwiply/ui`; services (incl. `parseFile`) wired via `use-resume-upload-services`.
- [x] **W2.2** Shipped. PR #21 → CI all green (extension, web, **web-docker build+runtime-smoke**, api) → merged to `main` → **deployed to prod** (`0f8c2bc`). Two CI-only bugs were caught + fixed first (extension `npm ci` cmd.exe shell on Linux; web Dockerfile missing the root manifest in the build stage). **Live-verified:** kiwiply.com 200, API health UP, `/privacy` (nested-standalone static) 200, `/resumes` 307→login. **W2 complete.** Remaining = eyeball the logged-in `/resumes` form once (needs a signed-in session; pure refactor so expected identical).

## Phase W3 — Extension side panel renders the shared form
> The on-the-fly upload opens a native right-side panel with the **real** form (React, in WXT).
- [x] **W3.1** `entrypoints/sidepanel/` (React: `index.html`+`main.tsx`+`App.tsx`+`style.css`) — WXT **auto-wires** `side_panel.default_path: "sidepanel.html"` + the `sidePanel` permission (no manual manifest edits). First React surface in the extension: `job-autofill` joined the workspace (the WXT half of W1.4), added `@wxt-dev/module-react` + react@19.2.4 + `@tailwindcss/vite`; `wxt.config` got `modules:["@wxt-dev/module-react"]` + a Tailwind v4 Vite plugin. The panel CSS imports `@kiwiply/ui/styles/tokens.css` + `@source`s the package — verified the built panel CSS carries the brand tokens (`--accent`) + token utilities (`bg-paper`/`text-ink`). W3.1 body is a styled placeholder (the `<ResumeUpload>` mount is W3.2). Membership ripple handled: web Dockerfile copies `job-autofill/package.json`; extension CI + publish jobs install workspace-aware at root; `job-autofill/package-lock.json` removed. `wxt build` + extension `npm test` + web `next build` all green.
- [x] **W3.2** Side panel mounts the SHARED `<ResumeUpload>`. `main.tsx` loads the engine (`engine.ts` side-effect-imports the JAF libs → `window.JAF`); `App.tsx` reads the popup handoff (file from the IndexedDB temp key + meta from `chrome.storage.local["pendingResumeReview"]`), builds a `File`, and mounts `<ResumeUpload embedded initialFile … onClose {...services}>`. `panel.ts` provides the services: `parseFile` → `JAF.parser.parse` (bio empty → contact panel hidden), `onSave` **save** → `createResume`+`uploadResumeFile`+`pullAll`. Loading/empty/done states. Added a real `tsconfig.json` (extends WXT's) + `@types/chrome` + a `typecheck` script now gating extension CI. `tsc`+`wxt build`+extension `npm test` green. *(attach = W3.3; popup still opens the old review tab until W3.4.)*
- [x] **W3.3** Extension `onSave` **attach** mode implemented in `panel.ts` (ported from `review.js` `attachAndFill`): `ensureInjected(jobTabId)` (executeScript `content-scripts/content.js`) → `JAF_CAPTURE_JOB` → `JAF.appTracking.pushDraft` → `provider.uploadApplicationAttachment(appId, file, fileName)` → `JAF.schema.buildFillValues` + base64 file → `pickFrame` → `JAF_FILL` on the best frame → focus the job tab. Cross-tab helpers (`sendTo`/`ensureInjected`/`pickFrame`/`blobToBase64`) lifted into `panel.ts`. `tsc`+`wxt build`+`npm test` green. **Both modes (save + attach) now go through the shared form.**
- [x] **W3.4** Popup `openReview` now opens the SIDE PANEL instead of the review tab: `chrome.sidePanel.open({ tabId })` called **synchronously** in the click gesture (tab id pre-fetched in `init` into `activeTabId` so no `await` precedes `open()`), then writes the handoff (file → IndexedDB temp key + meta → `chrome.storage.local`). Dropped the popup's pre-parse + pre-auth-check (the panel parses via `parseFile` and checks auth on save). The panel handles the open→write **race** + re-uploads via `chrome.storage.onChanged` (+ a 4s empty fallback). **Removed the vanilla review tab** (`entrypoints/review/` + `src/review/review.js`). `tsc`+`wxt build` (no more `review.html`)+`npm test` green. **The panel is now reachable in Chrome → manual walkthrough of both modes is the remaining gate.**
- [x] **W3.5** Panel states rounded out in `App.tsx`: **loading → ready (the form) → done**, plus **empty** (opened with no handoff) and **error** (load failure). Cancel → `onClose` → done; the **done** view confirms save vs cancel (`onSaved` sets a ref read in `onClose` → "All set ✓" vs "Closed") and attempts `window.close()`. Parse/save spinners + save errors are the form's own (embedded overlay / `saveError`). `tsc`+`wxt build`+`npm test` green. **Code-complete.** *Remaining = the manual both-modes walkthrough in Chrome (load `.output/chrome-mv3`), the user's gate. Deferred to W5: a `toast` surface (success is currently silent → the "done" view covers it) + mode-aware Save-button copy.*

## Phase W4 — Convert popup + options to React
> The functional port to React (so the visual overhaul in W5 has React surfaces to polish).
- [x] **W4.1** Popup converted to React on the shared design tokens (Tailwind). `entrypoints/popup/`: `index.html` (React shell) + `main.tsx` (loads `engine.ts` → `window.JAF`, then renders) + `PopupApp.tsx` (resume picker + meta, auto-advance, Scan-&-fill, Save-this-job, upload → save/attach choice, header links, bio nudge, status) + `actions.ts` (the engine logic — `refreshMirror`/`loadData`/`fillPage`/`saveJob`/`openReview` + inject/frame helpers) + `style.css` (tailwindcss + `@kiwiply/ui` tokens). Popup engine is now lean (storage/schema/tracking/sync — no parser/mammoth, since the panel parses). Removed `src/popup/*` + the old `main.js`. `tsc`+`wxt build`+`npm test` green (popup chunk 27kB→10.5kB). *Visual polish + fuller primitive adoption = W5.*
- [x] **W4.2** Options page converted to React on the shared tokens. `entrypoints/options/`: `index.html` (React shell, keeps the `open_in_tab` meta → `options_ui.open_in_tab:true`) + `main.tsx` + `OptionsApp.tsx` (Account connect/sign-out status, AI drafting toggles + key, Filling toggles + analytics + Save, Bug report with `#bug` deep-link scroll/focus) + `actions.ts` (`loadSettings`/`saveSettings`/`readAccount`/`signOut`/`sendBug`) + `engine.ts` (storage + tracking) + `style.css`. Live account refresh via `chrome.storage.onChanged`. Removed `src/options/*` + `main.js`. `tsc`+`wxt build`+`npm test` green. **W4 complete — popup, options, and sidepanel are all React now; `src/` is engine-only.** *Visual polish = W5.*
- [ ] **W4.3** (Future UI *features* slot in here as their own `w4.x` tasks; visual polish lives in W5.)

## Phase W5 — UI overhaul & design polish (Simplify-scale)
> Lift every extension surface to a polished, cohesive, Simplify-scale standard on the **shared
> design system**. Builds on the React conversion (W4) + `packages/ui` (W1). This is the "improve UI"
> track — visual/UX quality, not new plumbing.
- [x] **W5.1** Grow `packages/ui` into a real **design system**: tokens (color/shape/elevation, light **+ dark** — dark is opt-in via `.dark`/`[data-theme=dark]`) now the **single source** (web's `globals.css` imports the package tokens instead of duplicating), plus shared primitives (Button, Input, Select, Field, Card, Badge, Tabs, Toast, Skeleton, Spinner, EmptyState, Dialog, SidePanel shell, Tooltip, Menu) — dependency-free, on the shared tokens. One source for web + extension.
- [x] **W5.2** Redesigned the **popup** into a polished home surface on the design system (`Button`/`Select`/`Field`/`Badge`/`Spinner`): clearer header (logo + Manage + gear), resume picker with meta **badges** (skills/roles/file), upload entry, an auto-advance **toggle switch**, prominent primary actions (accent Fill + ghost Save), a status line with a spinner, and a trust footer (+ bug link). `actions.ts` engine logic unchanged; fills edge-to-edge on the paper token (dark-ready).
- [x] **W5.3** Redesigned the **options/settings** app on the design system: a sectioned **nav rail** (Account/AI/Filling/Feedback) beside consistent `Card` sections, all controls unified (`Switch`/`Field`/`Input`/`Select`/`Button`, account status as a `Badge`). Added a shared **`Switch`** primitive (`role=switch`, a11y) and retrofit the popup toggle to it (one implementation). `#bug` deep-link + single Save preserved; `actions.ts` unchanged.
- [x] **W5.4** Polished the **side-panel review**: mode-aware primary copy (attach → "Fill page & attach" vs "Save to my account") via new optional `saveLabel`/`savedToast` props on the shared `ResumeUpload`, a real **toast** surface (side panel wrapped in `ToastProvider`, `toast` service wired — the surface W3 deferred), and design-system state screens (`EmptyState`/`Spinner` for loading/empty/error/done). Closed both carried wrinkles. The **injected on-page panel** (Shadow-DOM overlay in `filler.js`) got a token consistency fix (`--muted` synced to the AA-corrected value); deeper on-page restyle is left for visual QA (needs a real ATS). Web unaffected (props optional).
- [x] **W5.5** State + interaction polish across surfaces: shared reduced-motion-safe **`animations.css`** (fade-in/slide-up) imported by popup/options/side-panel; **popup loading skeleton** (`Skeleton` while the mirror pulls, `aria-busy`) + mount fade-in; **options nav scroll-spy** (IntersectionObserver highlights the active section, `aria-current`) + content fade-in; keyed **fade-in on side-panel state screens**; `Skeleton` respects `prefers-reduced-motion`. (Focus rings + keyboard nav already come from the W5.1 primitives; full a11y/dark pass is W5.6.)
- [x] **W5.6** **Dark mode** wired across the surfaces via a manual **Light / System / Dark** toggle (new *Appearance* section in options): `lib/theme.ts` applies `.dark` on `<html>` from a persisted pref (own `themePref` key), follows the OS on *System*, and syncs across open surfaces; `initTheme()` runs in each `main.tsx` (sync system-apply before render → flash-min). On-page overlay stays light (it lives on the ATS page). **A11y** pass: persistent `aria-live` status regions (popup + options), `radiogroup`/`aria-current` semantics, `aria-busy` on the popup skeleton, verified names/contrast on the dark palette.
- [ ] **W5.7** Visual QA against the web app for cohesion (light + dark); before/after screenshots in the PR. **Checklist ready: `job-autofill/W5-QA.md`** — the one manual gate (surfaces need `chrome.*`, not web-previewable). Walk it in Chrome, log findings, fix as `w5.7: fix …` commits, then check this box.

## Phase W6 — Firefox parity, cleanup, docs, ship
- [ ] **W6.1** Firefox build via WXT (sidebar/injected panel for the side-panel surface); test.
- [ ] **W6.2** Remove dead vanilla assets; prune unused CSS.
- [ ] **W6.3** Update `ARCHITECTURE.md`, `PROGRESS.md`, this plan's checkboxes.
- [ ] **W6.4** Bump extension version, full test pass (web + extension), package, **CWS upload**.

---

## Key risks & decisions
- **Engine modules under Vite:** they self-attach to `globalThis.JAF` via IIFEs. Load via side-effect imports; the node test suite is unchanged (files untouched). Validate in W0.5 before going further.
- **Manifest `key`:** preserved in `wxt.config.ts` so the extension ID stays stable (unpacked == published) — keeps the `/connect` handoff + any ID-based config working.
- **MV3 no remote code:** everything is bundled/packaged (WXT/Vite output) — compliant.
- **CWS artifact (decision revised 2026-06-30):** the WXT build output (`.output/`) is **gitignored, not committed** — it's deterministic from source + lockfile, so committing it only adds hashed-chunk churn + `vendor/` duplication. **CI rebuilds and produces the release zip** (`publish-extension.yml`); reproducibility comes from CI artifacts on `ext-v*` tags, not from git. *(Supersedes the earlier sf0.0 "commit artifact" decision, which was sized for the old no-build extension where source == loadable artifact.)*
- **Incremental safety:** W0 ships the *same* extension on WXT (parity) before any UI is rewritten; popup/options stay vanilla until W4.
- **Gate fix in flight:** the connection-gate fix (`5d2342a`) rides this branch; cherry-pick to `main` sooner if you want it in prod before this build lands.
- **Bundle size / Tailwind:** share tokens with web `globals.css`; tree-shake; acceptable for panels.

## Definition of done (build-out)
WXT builds the extension (engine intact, parity verified); `packages/ui` holds one `ResumeUpload` +
a shared **design system** consumed by web + extension; the on-the-fly upload reviews in a right-side
panel using that form (both modes); **every extension surface is overhauled to a polished, cohesive,
Simplify-scale standard (W5) — consistent tokens, dark mode, accessibility**; web parity deployed;
extension shipped via a CWS upload; docs updated. The platform is ready for further Simplify-scale
UI in `entrypoints/` + `packages/ui`.

## Log
- 2026-07-01 · **ext UI fix pass** (user request, 8 items) — (1) `Select` wrapper is now `w-full` so the field (and its native dropdown) always spans its container; (2) **real Kiwiply logo artwork** everywhere — new `job-autofill/lib/Brand.tsx` (`BrandLogo`/`BrandMark`) renders the bundled `icons/logo.png` (+ `logo-dark-mode.png`, copied from `brand/`, `.dark`-swapped) via `chrome.runtime.getURL`, replacing the CSS `BrandLockup` (which needed the unbundled Fraunces font) in popup/options/side-panel headers — matches the on-page review panel, which already used `logo.png`; (3) cleaner **settings gear** icon (Lucide-style cog) in the popup; (4) unified **header structure** across surfaces (real logo + a muted context subline, echoing the review panel's `logo + "…detected · N fields ready"`); (5) **connect prompt now gates on account-connected** (reads `trackingAuth`), not on bio presence, so it hides the moment you sign in (+ live `storage.onChanged`); (6) popup header **shows the signed-in account** ("Signed in as …"); (7) **light is now the default** appearance (`lib/theme.ts` `DEFAULT_PREF="light"`; System/Dark still available in options); (8) options page — dropped the redundant "SETTINGS" eyebrow and un-wrapped the intro sentence (removed `max-w-[56ch]`). Version bumped **0.28.0 → 0.29.0** (popup/options/side-panel are user-visible; still unpublished — CWS upload rides W6.4). Gates: ext `typecheck`/`build` (logos bundled) + engine tests (14 suites) + UI/web `tsc` green.
- 2026-06-30 · Re-cast from `SHARED-FORM-PLAN.md` to a WXT platform plan (framework = WXT, engine stays). Decisions locked.
- 2026-06-30 · **w0.1** done — WXT installed (`wxt@^0.20.27`) + `job-autofill/.npmrc` shell fix. Next: **W0.2** (`wxt.config.ts` mirroring the manifest + relocate entrypoints → parity).
- 2026-06-30 · **w0.2 (full W0 parity push)** done — `wxt.config.ts` mirrors the manifest exactly (key preserved → stable ext ID); `entrypoints/` for background/content/popup/options/review (engine imported as-is, no React); `vendor/`+`icons/`→`public/`. `wxt build` is green and the generated manifest is at parity (permissions, hosts, externally_connectable, content_scripts→one bundled file, WAR, `options_ui.open_in_tab`). Extension test suite green (14). Build output (`.output/`) gitignored — derivable from source; CI rebuilds + zips (artifact decision revised, see Risks). Engine files untouched (only the SW `importScripts` line moved to the entrypoint; popup/review `executeScript`+`getURL` paths repointed at WXT outputs). **Left:** the manual load-unpacked walkthrough in Chrome, then **W1** (shared UI package). No version bump — build-system change, no shipped behavior change, nothing published (CWS upload stays manual at W6.4).
- 2026-06-30 · **w1.1** done — npm workspaces at the repo root (`workspaces: ["packages/*"]`) + `@kiwiply/ui` with `styles/tokens.css` (canonical brand tokens, byte-identical to web's inline copy). Additive only: web + extension are NOT workspace members yet (verified their isolated `npm ci` is unchanged → live web build untouched); they consume the tokens + join the workspace + get Docker/CI changes in **W1.4**. Next: **W1.2** — make web's `ResumeUpload` portable (inject `onSave`/`onCancel`/`track`; drop `next/*` + direct `fetch`), zero web behavior change.
- 2026-06-30 · **w1.2** done — `ResumeUpload` is now presentational: injected `ResumeUploadServices` (`onSave`/`onUpdateProfile`/`track`/`toast`/`onRefresh`), dropped `useRouter`/`useToast`/`track`-import/3×`fetch`; web wiring centralized in `use-resume-upload-services.ts` and spread by both callers; base-profile panel gated on `onUpdateProfile`. `tsc`+`eslint`+`next build` green; zero web behavior change. Next: **W1.3** — move `ResumeUpload` + sub-parts + the UI primitives it needs + `parser-core` types into `@kiwiply/ui`.
- 2026-06-30 · **w1.3 (+ W1.4 web half + W2.1)** done — moved `ResumeUpload` into `@kiwiply/ui` (self-contained primitives + `parser-core` types; `parseResume`→injected `parseFile`); web now consumes the package as a workspace member (transpilePackages, repo-root Turbopack/tracing → nested standalone, Tailwind `@source`); web **Dockerfile + CI** rewritten workspace-aware + a CI `web-docker` smoke job; `web/package-lock.json` removed. Web `tsc`+`eslint`+`next build` green, extension green. Next: **W2.2** — preview visual parity + confirm CI (incl. `web-docker`) green, then merge web → deploy. The WXT-consume half of W1.4 rides **W3** (extension side panel).
- 2026-07-01 · **ui-match pass** — aligned the shared `@kiwiply/ui` primitives 1:1 with the web app's real `web/src/components/ui/*` so the extension reads as the same product (the web imports its own copies, so this only affects the extension + `ResumeUpload`, which uses only Button/Input): **Switch** → web's 26×44 track w/ `after:` thumb; **Badge** → uppercase `ready/review/default` chips + new **`Pill`**/**`Tag`**; **EmptyState** → web's dashed-border surface (icons bumped, side panel no longer double-wraps it in a Card); **Select** → shared `inputClass` + `▾` caret; **Field** → web's 12.5px label + 15px rhythm (kept the a11y auto-wiring); **Card** padding `p-5`→`p-6`; **Toast** → web's bottom-right stack, `border-l-4`, `default/success/error` variants. Surfaces updated to the new variants: popup meta is now muted "N skills · N roles" text + a file-status `Badge`; options nav-active + account badge use the web `accent-soft/ready/review` treatment; side-panel toast passes `success/error` straight through. Also aligned React deps earlier this session (dupe/#527 fix). Gates: UI + web `tsc` + **web build**, ext `typecheck`/`build` (single React 19.2.4), engine tests (6 suites) green. No version bump (unshipped; ship at W6.4).
- 2026-07-01 · **w5.7 prep** — wrote the visual-QA walkthrough checklist **`job-autofill/W5-QA.md`** (load unpacked; popup / options / side-panel review / on-page overlay in **light + dark**; dark-mode sync; a11y/keyboard/reduced-motion; W0 parity re-confirm; screenshots for PR #22) and rebuilt `.output/chrome-mv3` fresh. W5.7 itself (running the walkthrough + screenshots) is the **user's manual gate** — the box stays unchecked until it's walked; any issues land as `w5.7: fix …` commits. This is the last W5 task; after it, **W5 is complete → W6** (Firefox parity, cleanup, docs, ship).
- 2026-07-01 · **w5.6** done — **dark mode** (manual toggle) + **a11y** pass. New **`job-autofill/lib/theme.ts`** (kept OUT of `entrypoints/` — WXT treats bare `.ts` there as an unlisted-script entrypoint): `resolveTheme`/`applyResolvedTheme` toggle `.dark` on `<html>`; `get/setThemePref` persist a `themePref` key in `chrome.storage.local`; `initTheme()` applies the OS default synchronously (flash-min), refines from the stored pref, and listens for OS `prefers-color-scheme` changes (while on *System*) + cross-surface `chrome.storage.onChanged` so a change in options reflects in an open popup/panel. `initTheme()` is called in all three `main.tsx` before render. Options gained an **Appearance** section (5th nav item) with a segmented **Light / System / Dark** radiogroup (sun/monitor/moon icons). The dark palette flips automatically because `@theme inline` resolves utilities to `var(--paper)` etc., which `.dark` overrides (W5.1). The **on-page overlay stays light** (shadow-DOM `:host` tokens; it lives on the ATS page, not an extension theme surface). **A11y:** popup + options status messages are now persistent `role="status" aria-live="polite"` regions; the theme control is a proper `radiogroup`; popup loading skeleton has `aria-busy`; nav uses `aria-current`. No version bump (unshipped; ship bump at W6.4). Gates: ext `wxt prepare`/`tsc`/`build` + engine tests (6 suites) green (UI/web untouched this task). Next: **W5.7** — visual QA vs the web app (light + dark), before/after screenshots in the PR — needs the Chrome walkthrough.
- 2026-07-01 · **w5.5** done — cross-surface **state + interaction polish**. Added a shared, reduced-motion-safe **`packages/ui/styles/animations.css`** (`.kiwi-fade-in`/`.kiwi-slide-up`; `@media (prefers-reduced-motion: reduce)` → no animation), exported it (`./styles/animations.css`), and imported it in all three surfaces' `style.css`. **Popup:** a real **loading skeleton** (`Skeleton` for the picker + meta while `refreshMirror`/`loadData` run, `aria-busy`) replacing the empty disabled picker, plus a mount `kiwi-fade-in`. **Options:** **scroll-spy** nav — an `IntersectionObserver` highlights the in-view section's link (`aria-current`, active pill), plus a content `kiwi-fade-in`. **Side panel:** the state screens now fade in on transition (`key={status}` + `kiwi-fade-in`). `Skeleton` primitive gained `motion-reduce:animate-none`. No engine changes; no version bump (unshipped; ship bump at W6.4). Gates: UI `tsc` + web `tsc` + ext `typecheck`/`build` + engine tests (6 suites) green. Next: **W5.6** — accessibility pass + wire **dark mode** across the surfaces (the tokens are ready from W5.1).
- 2026-07-01 · **w5.4** done — polished the **side-panel review** + closed the two W3-deferred wrinkles. Shared `ResumeUpload` (`packages/ui`) gained two **optional, backward-compatible** props: `saveLabel` (overrides the create-button copy) and `savedToast` (overrides the create-success toast copy) — web passes neither, so it's unchanged. Side panel (`entrypoints/sidepanel/`): `main.tsx` now wraps the app in **`ToastProvider`**; `App.tsx` calls `useToast()` and passes a `toast` service (maps ResumeToast success/error → ok/danger) so success is no longer silent, plus **mode-aware copy** (attach → "Fill page & attach" + "Filling this page…/Attaching your PDF…"; save → defaults). Panel state screens rebuilt on the design system (`EmptyState` + inline icons for empty/error/done, `Spinner` for loading). Injected on-page overlay (`src/content/filler.js`, Shadow-DOM): synced the drifted `--muted` to the AA-corrected `#686962`; the overlay was already token-based, and a deeper restyle needs a real ATS (visual QA), so it's intentionally minimal. Gates: UI `tsc` + web `tsc`/**build** + ext `typecheck`/`build` + engine tests (6 suites) green. No version bump (unshipped; ship bump at W6.4). Next: **W5.5** — state + interaction polish across all surfaces (loading/empty/error/success, focus, keyboard nav, transitions).
- 2026-07-01 · **w5.3** done — redesigned the **options/settings** page (`entrypoints/options/OptionsApp.tsx`) on the design system. Layout: a two-column shell with a sticky **sectioned nav rail** (Account · AI drafting · Filling · Feedback, anchor links that scroll to `scroll-mt` sections) beside a column of `Card`s (collapses to single column on narrow). Controls unified: every toggle is the new shared **`Switch`**, the API key is `Field`+`Input`, the bug category is `Select`, the bug message reuses `inputClass` on a textarea, actions are shared `Button`s, and the account state shows a `Badge` (Connected/Not connected). Added **`packages/ui/src/primitives/Switch.tsx`** (`<button role="switch">`, labeled-row or bare, keyboard + aria-checked) to the barrel, and **retrofit the popup's inline toggle to it** → one switch implementation (popup chunk 14→11 kB from the dedupe). `#bug` deep-link preserved (now via `getElementById`, since `Card` isn't a forwardRef) and the single Save still persists the whole settings form; `actions.ts` untouched. Gates: UI `tsc` + web `tsc` + ext `typecheck`/`build` + engine tests (6 suites) green. No version bump (unshipped; ship bump at W6.4). Next: **W5.4** — polish the side-panel review + the injected on-page autofill panel.
- 2026-06-30 · **w5.2** done — redesigned the **popup** (`entrypoints/popup/PopupApp.tsx`) on the W5.1 design system. Header: logo + a "Manage" link + a gear (Settings) icon button (dropped the emoji row; bug report moved to a subtle footer link). Resume picker: shared `Field`+`Select`; selected-resume meta rendered as `Badge`s (skills · roles · file ✓/no-file with ok/warn variants); an empty hint when there's no resume. Auto-advance is now a real **toggle switch** (peer-styled checkbox). Primary actions use the shared `Button` (accent "Scan & fill", ghost "Save this job") with an inline `Spinner` while busy; status line is color-tokened (danger/accent-deep/muted). Popup fills edge-to-edge on the `--paper` token (was a transparent-body rounded card) → dark-ready. **No engine change** (`actions.ts` untouched) and **no version bump** (unshipped branch; single ship bump at W6.4, per W0–W4). Ext `typecheck`+`build` green (popup chunk 10.5→14 kB as it pulls in the primitives). Visual gate = the Chrome walkthrough (popup needs `chrome.*`, so it's not web-previewable). Next: **W5.3** — redesign the options page on the system.
- 2026-06-30 · **w5.1** done — grew `@kiwiply/ui` into a real **design system**. (1) **Tokens light + dark**: added an opt-in dark palette to `styles/tokens.css` under `.dark`/`[data-theme=dark]` (inverts surfaces/text, keeps the kiwi accent; `@theme inline` means every `bg-*/text-*/border-*` utility flips with no per-utility work) + `--radius-sm`/`--shadow-sm`; web is unchanged (never sets the class). (2) **Single source**: web's `globals.css` now `@import`s the package tokens instead of the ~80-line inline duplicate (byte-identical light values → zero visual change; `next build` confirms the CSS `@import` + `@theme` resolve under Turbopack/Tailwind). (3) **Primitives** (dependency-free, matching the Button/Input idiom, all on the shared tokens): Select, Field (label/hint/error + aria wiring), Card (+Header/Title/Description/Footer), Badge, Tabs (roving-tabindex a11y), Toast (`ToastProvider`+`useToast`, self-contained keyframe — the surface W3 deferred), Skeleton, Spinner, EmptyState, Dialog (focus trap/Esc/scroll-lock, no portal), SidePanel shell, Tooltip (CSS-only), Menu (click-outside/arrow-nav). Barrel re-exports the lot. Gates: UI `tsc` + web `tsc`/**build** + ext `typecheck`/**build** + engine tests (all 6 suites) green. No version bump — no surface consumes the new primitives yet (bundle tree-shakes them out), so no shipped-behavior change. Next: **W5.2** — redesign the popup on the new system.
- 2026-06-30 · **w4.2** done — options page converted to React (`entrypoints/options/`: `OptionsApp.tsx` + `actions.ts` + `engine.ts` + Tailwind `style.css`; `open_in_tab` preserved; live account refresh via `storage.onChanged`). Removed `src/options/*`. **Phase W4 complete** — popup + options + sidepanel are all React; `src/` is now engine-only (background/config/content/lib). `tsc`/build/tests green. Next: **W5** — the UI overhaul (design system, dark mode, a11y) across all React surfaces.
- 2026-06-30 · **w4.1** done — popup converted to React on the shared tokens (`entrypoints/popup/`: `PopupApp.tsx` UI + `actions.ts` engine logic + `engine.ts` lean loader + Tailwind `style.css`). Removed `src/popup/*`. `tsc`/build/tests green. Next: **W4.2** — convert the options page to React (account/connect, AI toggles, filling, bug report).
- 2026-06-30 · **w3.5** done — panel states polished (`App.tsx`): loading/empty/**error**/ready/**done** (done confirms save-vs-cancel + attempts `window.close`). Toast surface + mode-aware copy deferred to W5. `tsc`/build/tests green. **Phase W3 is code-complete** — the extension renders the shared form in a side panel for both save + attach. Remaining W3 gate = the manual both-modes walkthrough in Chrome. Next phase: **W4** (convert popup + options to React) or the merge/ship track.
- 2026-06-30 · **w3.4** done — popup opens the side panel (`chrome.sidePanel.open` sync in the gesture, tab id pre-fetched) + writes the handoff; panel handles the race/re-upload via `storage.onChanged`. Dropped the popup pre-parse/pre-auth; **removed the vanilla review tab** (`entrypoints/review/`, `src/review/review.js`). Build has no `review.html`. `tsc`/build/tests green. **The panel is reachable in Chrome now** — next: **W3.5** states polish + the manual both-modes walkthrough (load `.output/chrome-mv3`).
- 2026-06-30 · **w3.3** done — the attach-and-fill `onSave` branch in `panel.ts` (ported from `review.js`): capture → `pushDraft` → `uploadApplicationAttachment` → fill the job tab → focus. Both save + attach now run through the shared form. `tsc`/build/tests green. Next: **W3.4** — popup opens the side panel (`chrome.sidePanel.open`) + writes the handoff, and drop the old `src/review/*` tab; only then is the panel reachable/testable in Chrome.
- 2026-06-30 · **w3.2** done — the side panel mounts the SHARED `<ResumeUpload>` (React): engine loaded into the panel (`window.JAF`), reads the popup handoff (IndexedDB file + `pendingResumeReview` meta), extension services `parseFile` (→ `JAF.parser`) + `onSave` (save → `createResume`+`uploadResumeFile`+`pullAll`). Added extension `tsconfig.json` + `@types/chrome` + a `typecheck` CI gate. `tsc`/build/tests green. Next: **W3.3** — the attach-and-fill onSave mode. Popup opens the panel + drops the old review tab in W3.4.
- 2026-06-30 · **w3.1** done — extension's first React surface: `entrypoints/sidepanel/` (React + Tailwind v4), WXT auto-wired `side_panel` + `sidePanel` perm. `job-autofill` joined the npm workspace (WXT half of W1.4) — `@wxt-dev/module-react` + react 19 + `@tailwindcss/vite`; panel imports `@kiwiply/ui/styles/tokens.css` (+`@source`), verified tokens compile into the panel CSS. Body is a placeholder; `<ResumeUpload>` mounts in W3.2. Membership ripple handled (web Dockerfile copies job-autofill manifest; ext CI + publish install workspace-root). All builds/tests green. Next: **W3.2** — read the popup handoff + mount `<ResumeUpload>` with extension services.
- 2026-06-30 · **w2.2 (DEPLOYED)** — PR #21 merged → prod (`0f8c2bc`). Fixed two CI-only bugs first (extension `npm ci` cmd.exe shell; Docker build-stage missing root manifest) + upgraded the CI `web-docker` job to also smoke-RUN the container (validates the nested-standalone runtime). All CI green; live-verified (web 200, API UP, nested-standalone static 200, gated 307). **Phase W2 done.** Next: **W3** — extension side panel renders the shared form (job-autofill joins the workspace + consumes `@kiwiply/ui` — the WXT half of W1.4).
