node_modules/
npm-debug.log*

# WXT generated types/scaffolding (regenerated by `wxt prepare`).
.wxt/

# Dev builds + zip bundles. The PRODUCTION build (.output/chrome-mv3) is committed as the
# CWS/dev-load artifact per the locked artifact decision; transient dev outputs are not.
.output/*-dev/
.output/*.zip
