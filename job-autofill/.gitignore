node_modules/
npm-debug.log*

# WXT generated types/scaffolding (regenerated by `wxt prepare`).
.wxt/

# Build output is derivable from source + lockfile (deterministic `wxt build`); CI rebuilds
# and produces the release zip. Not committed — avoids hashed-chunk churn + vendor duplication.
.output/
